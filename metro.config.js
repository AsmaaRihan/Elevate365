const { getDefaultConfig } = require('expo/metro-config');
// If it's a plain React Native project (not Expo), use:
// const { getDefaultConfig } = require('metro-config');

module.exports = getDefaultConfig(__dirname);
